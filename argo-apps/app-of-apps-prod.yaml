apiVersion: argoproj.io/v1alpha1
kind: Application
metadata:
  name: apps-root-prod
  namespace: argocd
spec:
  project: default
  source:
    repoURL: https://github.com/Kim-Min-Gyeong/21-iceT-gitops-eks
    targetRevision: main
    path: argo-apps/apps-prod
    directory:
      recurse: true
  destination:
    server: https://kubernetes.default.svc
    namespace: argocd
  syncPolicy:
    automated:
      prune: true
      selfHeal: true
